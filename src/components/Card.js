import styled from "styled-components";

const Card = styled.div`
  background: white;
  padding: 1rem 3rem;
  width: 100%;
  border-radius: 10px;
  box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.3);
  margin: 1rem 0;
`;

export default Card;
